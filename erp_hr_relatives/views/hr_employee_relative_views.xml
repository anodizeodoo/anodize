<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="employee_relative_relation_form" model="ir.ui.view">
        <field name="name">hr.employee.relative.relation.form</field>
        <field name="model">hr.employee.relative.relation</field>
        <field name="arch" type="xml">
            <form string="Parentesco">
                <group>
                    <field name="name"/>
                    <field name="code"/>
                    <field name="active" widget="boolean_toggle"/>
                </group>
            </form>
        </field>
    </record>

    <record id="employee_relative_relation_search" model="ir.ui.view">
        <field name="name">hr.employee.relative.relation.search</field>
        <field name="model">hr.employee.relative.relation</field>
        <field name="arch" type="xml">
            <search string="Parentesco">
                <field name="name" string="Título"/>
                <field name="code"/>
            </search>
        </field>
    </record>

    <record id="employee_relative_relation_list" model="ir.ui.view">
        <field name="name">hr.employee.relative.relation.list</field>
        <field name="model">hr.employee.relative.relation</field>
        <field name="arch" type="xml">
            <tree string="Parentesco">
                <field name="name"/>
                <field name="code"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <record id="open_employee_relative_relation_form" model="ir.actions.act_window">
        <field name="name">Parentescos</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hr.employee.relative.relation</field>
        <field name="view_mode">tree,form</field>
    </record>


</odoo>
